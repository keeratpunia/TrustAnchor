{
  "name": "@trustanchor/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared cryptographic primitives for TrustAnchor Engine 1: deterministic canonical CBOR encoding, SHA-256 hashing helpers, and the exact wire-format types (Credential Payload, Trust Manifest Payload, QR Payload) used identically by the offline-signer and the backend. See src/canonicalCbor.ts for why every rule exists.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --loader ts-node/esm --test src/**/*.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",
    "@types/node": "^20.12.7"
  }
}
